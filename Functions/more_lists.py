def greater_than(numbers, target):
    result = []
    for num in numbers:

        if num > single_num:
            result.append(num)
    return result


print(greater_than([2, 8, 3, 10, 15, 5], 6))